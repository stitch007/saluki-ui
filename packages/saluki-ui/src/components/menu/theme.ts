export interface MenuTheme {
  indent: string
  arrowSize: string
  arrowColor: string
  darkArrowColor: string
}

export const defaultMenuTheme: MenuTheme = {
  indent: '16px',
  arrowSize: '20px',
  arrowColor: '#333639',
  darkArrowColor: 'rgba(255, 255, 255, 0.82)',
}
